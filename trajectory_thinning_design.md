# 轨迹图像化与方向感知抽稀方案

## 1. 目标

大模型直接接收数百条轨迹的坐标序列时，文本 token 数量会快速增长。当前方案把轨迹绘制成图像，用视觉输入表达轨迹的位置、形状和行驶方向。

项目包含两个对照脚本：

- `2.py`：不做轨迹抽稀，绘制全部有效行驶轨迹，用作基线。
- `1.py`：在绘图前合并位置相近且方向一致的轨迹，只绘制每组代表轨迹。

抽稀必须满足两个条件：

1. 空间位置足够接近。
2. 行驶方向一致。

因此，同一道路上方向相反的轨迹不会被合并。

## 2. 文件说明

| 文件 | 用途 |
| --- | --- |
| `D:\wsl\traj\merged_trajectories.npy` | 输入轨迹数据 |
| `D:\wsl\1.py` | 方向感知抽稀并绘图 |
| `D:\wsl\2.py` | 不抽稀，绘制全部有效轨迹 |
| `D:\wsl\directionl_trajectory_arrow.png` | 抽稀后的轨迹图 |
| `D:\wsl\directionl_trajectory_arrow_before_optimization.png` | 抽稀前的轨迹图 |
| `D:\wsl\trajectory_thinning_manifest.json` | 抽稀分组、原始索引和验证结果 |

## 3. 支持的 NPY 数据格式

### 3.1 固定长度数组

所有轨迹点数相同时，数据形状可以是：

```python
data.shape == (N, T, 2)
data.dtype == np.float32
```

其中：

- `N`：轨迹数量。
- `T`：每条轨迹的坐标数量。
- 最后一维 `2`：像素坐标 `[x, y]`。

### 3.2 变长 object 数组

每条轨迹点数不同时，数据形状可以是：

```python
data.shape == (N,)
data.dtype == object
data[i].shape == (Li, 2)
```

其中每条轨迹可以有不同的 `Li`。这种格式需要：

```python
data = np.load(path, allow_pickle=True)
```

输入文件应来自可信来源，因为 object NPY 使用 Python pickle 序列化。

## 4. 数据加载与清洗

`1.py` 和 `2.py` 使用完全相同的数据加载与清洗规则，保证对比公平。

对每条轨迹依次执行：

1. 转换为 `float32`。
2. 检查形状必须为 `(Li, 2)`。
3. 删除包含 `NaN` 或无穷值的坐标。
4. 删除连续重复坐标。
5. 少于两个不同坐标的轨迹视为静止轨迹，不参与绘制和抽稀。
6. 保存原始 NPY 索引，写入抽稀清单。

当前数据统计：

| 阶段 | 数量 |
| --- | ---: |
| NPY 原始记录 | 597 |
| 零长度静止轨迹 | 40 |
| 有效行驶轨迹 | 557 |
| 被相似轨迹代表 | 249 |
| 最终保留代表轨迹 | 308 |

这些数字的关系是：

```text
597 - 40 = 557
557 - 249 = 308
40 + 249 + 308 = 597
```

`retained` 是 `moving` 的子集，不能计算 `moving + retained`。

## 5. 原始坐标与图像大小

代码不缩放、不平移轨迹坐标：

```text
scale  = 1.0
offset = [0.0, 0.0]
```

图像大小由有效轨迹的最大坐标确定：

```python
image_width  = ceil(max_x) + 1
image_height = ceil(max_y) + 1
```

当前数据生成的图像大小为：

```text
10281 x 6310
```

因此，原始轨迹中的坐标 `[x, y]` 会直接绘制到图像像素 `(x, y)`，不会产生尺度误差。

如果输入数据包含负坐标，代码会报错，因为负坐标无法在不平移的情况下直接映射为图像像素。

## 6. 轨迹等弧长重采样

不同轨迹可能包含不同数量的坐标，无法直接逐点比较。抽稀前将每条轨迹按弧长均匀重采样为 40 个点。

处理过程：

1. 计算相邻坐标间的欧氏距离。
2. 得到轨迹累计弧长。
3. 在 `[0, 总弧长]` 上均匀选取 40 个位置。
4. 通过线性插值得到 40 个对应坐标。

重采样不会反转轨迹顺序，因此起点、终点和行驶方向保持不变。

该步骤使短轨迹、长轨迹和不同采样频率的轨迹可以使用统一的比较方法。

## 7. 空间相似度

两条重采样轨迹 `A` 和 `B` 的对应点距离为：

```text
d_i = ||A_i - B_i||_2
```

当前空间判断条件为：

```text
平均距离 <= 200 px
P90 距离 <= 360 px
```

其中：

- 平均距离控制整体轨迹接近程度。
- P90 距离限制局部偏离，避免少数区域差异过大。
- `360 = 200 × 1.8`。

只使用平均距离可能让局部明显分叉的轨迹被错误合并，因此同时使用 P90 距离。

## 8. 方向一致性

对重采样轨迹计算相邻点形成的局部切向量：

```text
v_i = P_(i+1) - P_i
```

两条轨迹对应切向量的方向相似度使用余弦值：

```text
cos(theta_i) = (vA_i · vB_i) / (||vA_i|| ||vB_i||)
```

当前参数为：

```text
方向角阈值：30°
至少 85% 的局部切向量满足夹角 <= 30°
```

同一路径反向行驶时，对应切向量的余弦接近 `-1`，不会通过方向判断。

## 9. 分组策略

抽稀使用“代表轨迹直接匹配”的贪心分组：

1. 第一条有效轨迹建立第一个分组，并成为代表。
2. 后续轨迹依次与已有分组的代表轨迹比较。
3. 只有同时通过空间条件和方向条件时，才加入对应分组。
4. 如果没有匹配分组，则建立新分组。
5. 最终每组只绘制代表轨迹。

没有使用传递式并查集合并。传递合并可能出现：

```text
A 接近 B
B 接近 C
A 不接近 C
```

但 A、B、C 仍被放入同组的问题。当前方案要求每个成员都直接满足其代表轨迹的阈值，便于验证和解释。

## 10. 箭头与颜色

轨迹颜色由每个局部线段的方向角计算：

1. 使用 `atan2(dy, dx)` 得到方向角。
2. 将方向角映射为 HSV 色相。
3. 转换成 OpenCV BGR 颜色。
4. 使用 `cv2.arrowedLine` 绘制方向箭头。

不同方向会显示为不同颜色，即使轨迹位置接近，也能从颜色和箭头方向识别行驶方向。

当前设置：

| 参数 | `2.py` 原始版 | `1.py` 抽稀版 |
| --- | ---: | ---: |
| 线宽 | 4 px | 4 px |
| 箭头间距 | 45 px | 55 px |
| 箭头尖端比例 | 0.35 | 0.35 |

## 11. 正确性验证

`1.py` 每次运行都会执行验证，并把结果写入：

```text
D:\wsl\trajectory_thinning_manifest.json
```

### 11.1 真实分组验证

对每一个分组中的每一个成员，重新计算它与代表轨迹之间的：

- 平均位置距离。
- P90 位置距离。
- 局部方向一致率。
- 平均方向余弦。

只有全部成员满足设置的阈值，验证才通过。

当前验证结果：

| 指标 | 结果 | 要求 |
| --- | ---: | ---: |
| 阈值违规数量 | 0 | 必须为 0 |
| 最大平均距离 | 198.694 px | <= 200 px |
| 最大 P90 距离 | 359.434 px | <= 360 px |
| 最低方向一致率 | 87.2% | >= 85% |

### 11.2 正反向合成测试

代码额外构造三条测试轨迹：

1. 一条正向直线。
2. 与第一条相距 2 px 的同向直线。
3. 与第一条位置完全相同的反向直线。

预期结果：

- 两条相近同向轨迹应合并。
- 位置相同的反向轨迹不应合并。

当前结果：

```text
synthetic_same_direction_merged = true
synthetic_reverse_direction_merged = false
validation_passed = true
```

## 12. 抽稀清单结构

`trajectory_thinning_manifest.json` 保存：

```json
{
  "source_count": 597,
  "moving_count": 557,
  "retained_count": 308,
  "distance_threshold_px": 200.0,
  "direction_threshold_deg": 30.0,
  "resample_points": 40,
  "coordinate_transform": {
    "scale": 1.0,
    "offset": [0.0, 0.0],
    "image_width": 10281,
    "image_height": 6310
  },
  "validation": {
    "passed": true,
    "threshold_violations": []
  },
  "groups": [
    {
      "representative_index": 0,
      "source_indices": [0, 5, 8]
    }
  ]
}
```

其中索引对应原始 NPY 中的轨迹索引，可以追溯哪些轨迹被哪个代表轨迹覆盖。

## 13. 参数调整

### 13.1 空间阈值

```python
distance_threshold_px = 200.0
```

- 增大：合并更多轨迹，图像更稀疏，但可能合并相邻道路或相近转向。
- 减小：保留更多细节，结果更保守，但箭头重叠会增加。

建议根据原始坐标尺度调整。当前阈值使用原始像素单位，不是 512×512 缩放后的像素。

### 13.2 方向角阈值

```python
direction_threshold_deg = 30.0
```

- 减小：方向判断更严格。
- 增大：允许更大的局部方向差异。

### 13.3 方向一致率

当前代码要求：

```python
direction_agreement >= 0.85
```

表示至少 85% 的对应局部线段方向差不超过 30°。

### 13.4 重采样点数

```python
resample_points = 40
```

- 增大：比较更精细，计算量增加。
- 减小：计算更快，但可能遗漏局部形状差异。

### 13.5 箭头间距

```python
arrow_spacing_px = 55
```

增大该值可减少每条轨迹上的箭头数量，但不会改变轨迹抽稀分组。

## 14. 运行方式

使用当前目录的 UV 虚拟环境：

```powershell
D:\wsl\.venv\Scripts\python.exe D:\wsl\2.py
D:\wsl\.venv\Scripts\python.exe D:\wsl\1.py
```

建议先运行 `2.py` 生成未抽稀基线图，再运行 `1.py` 生成抽稀结果和验证清单。

## 15. 计算复杂度

假设有效轨迹数量为 `N`，每条轨迹重采样为 `K=40` 个点。

当前贪心分组最坏时间复杂度约为：

```text
O(N² × K)
```

空间复杂度约为：

```text
O(N × K)
```

对于数百条轨迹可以直接运行。如果未来轨迹数量达到数万条，可先按起点区域、终点区域和总体方向进行空间索引分桶，再在桶内执行精确比较。

## 16. 当前方案的边界

1. 轨迹必须使用二维坐标 `[x, y]`。
2. 轨迹点的排列顺序必须代表真实时间方向。
3. 负坐标在“不平移”的约束下无法直接绘制。
4. 极短但非零的轨迹可能保留，因为其方向信息有限。
5. 当前方法比较完整轨迹；只部分重叠但起终点明显不同的轨迹通常不会合并。
6. 抽稀目的是降低视觉重复，不代表删除原始数据；所有分组关系都可以通过 manifest 追溯。
